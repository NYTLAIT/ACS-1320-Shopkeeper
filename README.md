Base number demands:
coffee always sell
bagels are pretty good as well
tea never sells however

Base demand multiplier:
const dayMultiplier = 0.80 + Math.random() * 0.30
let dailySales = baseDemand * dayMultiplier

Base demand is 80-110% of preset demand which is a decent range of randomness
